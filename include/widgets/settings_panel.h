#ifndef SETTINGS_PANEL_H
#define SETTINGS_PANEL_H

#include "app.h"
#include "raylib.h"

/* TODO: Come up with a better name, so when settings would be added there would be no overlap */
void settings_panel_render(App *application, Vector2 position, Vector2 size);

#endif
